**This code was written to play around with some routing algorithms and to try different things. It might not be the best available routing solution**

Add an OSM File to:
`app/conf/`

And set the Path in the GraphProvider.java

Run with:
(You have to install sbt :/)
sbt run -Xms3072M -Xmx6144M -XX:+CMSClassUnloadingEnabled -XX:MaxPermSize=256M

visit:
http://localhost:9000/map



For Neo4j:

You should run:
`sudo ulimit 40000`
